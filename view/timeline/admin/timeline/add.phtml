<?php
/**
 * The add view for the Timelines administrative panel.
 */

$head = array('bodyclass' => 'timelines primary',
              'title' => html_escape(__('Timelines | Add a Timeline')));
echo head($head);

echo $form;

echo foot();

